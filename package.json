{
  "version": "2.1.1",
  "scripts": {
    "major": "npm version major",
    "minor": "npm version minor",
    "patch": "npm version patch",
    "postversion": "goreleaser --rm-dist; git push; git push --tags"
  }
}
